#include <stdio.h>

/**
 * main - Ikyk what this is.
 * @argc: Parameter count.
 * @argv: Parameter vectors.
 * Return: 0 (succy)
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
