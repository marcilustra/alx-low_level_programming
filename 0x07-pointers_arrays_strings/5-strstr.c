#include "main.h"

/**
* _strstr - locates a substring
* @haystack: larger string that is searched
* through
* @needle: substring being searched.
* Return: 0 (Succ)
*/

char *_strstr(char *haystack, char *needle)
{
	char *result = haystack, *fneedle = needle;

	while (*haystack)
	{
		while (*needle)
		{
			if (*haystack++ != *needle++)
			{
				break;
			}
		}
		if (!*needle)
		{
			return (result);
		}
		needle = fneedle;
		result++;
		haystack = result;
	}
	return (0);
}
