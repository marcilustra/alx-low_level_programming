#include "main.h"
#include <stdio.h>
/**
 * mul - Multiplies two integers
 * @a: first int passed
 * @b: second int passed
 * Return: 0 (Success) mulvalue
 */
int mul(int a, int b)
{
	int mulval;

	mulval = (a * b);
	return (mulval);
}
