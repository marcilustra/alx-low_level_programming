#include<stdio.h>
/*Main duh*/

/**
 *main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	/*Int initialization*/
	a = 0;
	b = 10;

	do {
		printf("%d\n", a);
		a++;
	} while (a < b);
	while (a = 0; a < b; a++)
	{
		 printf("%d\n", a);
	}

	return (0);
}
