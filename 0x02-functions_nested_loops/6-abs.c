#include "main.h"
/**
 * _abs - Prints out the absolute value of
 * an integer
 *
 * @n: The int passed as an argument
 *
 * Return: 0 (Absolute value of n)
 */
int _abs(int n)
{
	int abval = abs(n);

	return (abval);
}
