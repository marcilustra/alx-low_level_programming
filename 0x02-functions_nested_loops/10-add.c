#include "main.h"
/**
 * add - Adds 2 integers, a and b
 * thats it
 * @a: int one
 * @b: int two
 *
 * Return: 0 (Succ)
 */
int add(int a, int b)
{
	/*Variable that holds sum of a and b*/
	int sum = a + b;

	return (sum);
}
