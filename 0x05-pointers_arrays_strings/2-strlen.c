#include "main.h"
#include <string.h>
/**
 * _strlen - Returns the length of the string
 * but using the pointer to the string
 * @s: Pointer to string
 *
 * Return: Length of String (success)
 */
int _strlen(char *s)
{
	int p;

	p = strlen(s);
	return (p);
}
